# 测试结果摘要

- `pytest -q tests/tools/test_shell_reap.py tests/agent/test_subagent_lifecycle.py tests/agent/tools/test_goal_orchestration.py tests/agent/test_runner_injections.py`：122 passed。
- `ruff check nanobot/ tests/`：通过。
- `pytest -q`：5239 passed, 2 failed, 18 skipped, 1 deselected；失败为 `tests/agent/test_workspace_scope.py` 两项，运行环境 `/usr/bin/bash` 找不到 `python`。
- `cd webui && bun run test`：失败，`src/tests/thread-shell.test.tsx` 的 `inline-file-path` 断言未找到。
- `cd webui && bun run build`：通过。
- `cd webui && bunx playwright test --project=chromium`：3 passed, 1 failed；`audit-tool-recovery-real.spec.ts` 移动端 `real-event-0005` 行未找到。
- 已执行真实镜像构建和 `docker compose up -d --force-recreate nanobot-gateway`；未执行 skip/xfail 或降低断言。
