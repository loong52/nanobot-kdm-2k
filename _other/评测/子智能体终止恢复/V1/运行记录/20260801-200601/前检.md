# 前检

- 时间：2026-08-01T19:42:45Z 开始；硬时限：2026-08-01T20:17:46Z。
- 分支：`codex/eval-termination-v1`；HEAD：`c1759dd2f0a77f676c16e75e2c978bda8bb23c13`；远端仅记录 origin，未 fetch upstream。
- 初始工作区：`M webui/package-lock.json`（受保护，未触碰）。Gateway 容器初始 ID `fc29abc7…`，镜像 `sha256:9cec7a5f…`，8765 HTTP 200，18790 health 可用；Audit health 未带 bearer 返回 401。
- Chromium 可启动，bootstrap 返回 200；WebSocket 可建立。初始页面有 1 次 `ERR_BLOCKED_BY_RESPONSE.NotSameOrigin` console error。
- 解析配置：宿主默认 workspace `~/.nanobot/workspace`；容器实际 workspace `/home/nanobot/.nanobot/workspace`；audit root `/home/nanobot/.nanobot/audit/v1`；Audit `full`、index enabled、max concurrent subagents `1`。
- 容器重建后 ID `4a53e96f…`、镜像 `sha256:67106e8c…`，18790 health `{"status":"ok"}`；8765 后恢复可用。
- 正式 S05-S13 真实重启 scenario suite 未在 `tests/`、`nanobot/` 或 `webui/e2e/` 找到；现有 restart 测试使用模拟 runtime。按试卷归为产品缺失，不归环境失败。
